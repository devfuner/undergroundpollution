<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="kr.co.ejbt.undergroundpollution.home.HomeMapper">

    <sql id="query">
        SELECT
            *
        FROM home.home
    </sql>

    <sql id="where">
        <where>

        </where>
    </sql>

    <select id="list" resultType="kr.co.ejbt.undergroundpollution.home.Home">
        <include refid="query" />
        <include refid="where" />
    </select>

</mapper>